---
- name: Restart OpenClaw
  systemd:
    name: "openclaw-gateway-{{ openclaw_install_user }}"
    state: restarted
  ignore_errors: true

- name: Restart s1-dashboard
  systemd:
    name: s1-dashboard
    state: restarted
  ignore_errors: true
